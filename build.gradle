plugins {
    id 'com.android.application' version '8.1.0' apply false // Update to latest AGP version compatible with Gradle 8.7
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
